@@ .. @@
 import { defineConfig } from 'astro/config';

 export default defineConfig({
-  site: 'https://yourusername.github.io', // Replace with your GitHub username
+  site: 'https://zoidt.github.io',
   integrations: [],
   markdown: {
     shikiConfig: {
       theme: 'github-dark'
     }
   }
 });